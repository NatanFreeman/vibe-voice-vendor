# VibeVoice ASR Server — Required CLI Arguments
#
# All arguments are required. There are no defaults.
# Example invocation:
#
#   python -m server \
#     --vllm-base-url http://localhost:37845 \
#     --host 127.0.0.1 \
#     --port 54912 \
#     --max-audio-bytes 524288000 \
#     --max-queue-size 50 \
#     --jwt-public-key-file ~/Desktop/vibe-voice-vendor/keys/public.pem \
#     --revoked-tokens-file ~/Desktop/vibe-voice-vendor/revoked_tokens.txt \
#     --require-https true \
#     --vllm-model-name vibevoice \
#     --vllm-temperature 0.0 \
#     --vllm-top-p 1.0
#
# Generate key pair + token with:
#   uv run python -m scripts.generate_token --keys-dir keys --subject user
#
# Rust proxy (vvv_proxy) — all arguments required:
#
#   ./vvv_proxy \
#     --upstream-host 127.0.0.1 \
#     --upstream-port 54912 \
#     --listen-host 0.0.0.0 \
#     --listen-port 42862 \
#     --max-body-size 524288000 \
#     --cert-path certs/self-signed/fullchain.pem \
#     --key-path certs/self-signed/privkey.pem \
#     --cert-validity-days 3650 \
#     --cert-check-interval-secs 3600
